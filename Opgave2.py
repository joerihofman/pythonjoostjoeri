import random
#opdracht 2

listL = [30, 1, 2, 1, 0, "hello", "Goodbye"];
listM = [30, 1, 2, 1, 0];
# a
# showing the position in the list. It searches the value in the list.
#listL.index(1)


# b
# counting the total amount of the value entered.
#listL.count(1)

# c
# total amount of items in the list
#len(listL)

# d
# max gives max INT value. This has to be an int else
#max(listL)

# e
# append makes the value entered added to the list.
#listL.append(40)

# f
# The second value replaces the value in the list on the position from the first value.
#listL.insert

# g
# this adds the values at the end of the list.
# listL.extend([1,44])
print( listL.extend([1,44]))
print (listL)

#max(listL)



#l
# sorts the list from small to large
#(listM.sort()

#m
# randomises a list
#random.shuffle(listL)
